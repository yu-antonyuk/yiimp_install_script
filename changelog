Yiimp Install Scrypt v0.4.1 - (2022-10-16)


Information =>
* Create start.sh
     => New way for the installer to star.
     => The installer now helps user to create new user if run as root.
 
 * Update user input menu


Fix =>


Change =>
* Remove pool.conf from conf folder the script create it from now.
* Remove old unfinished builder.sh from source.
